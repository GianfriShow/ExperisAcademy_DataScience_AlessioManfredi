# modules are files containing variables, functions, classes that we can import in other modules or programmes and use later

import my_module

my_module.greet("Robert")

radius = 2
circle = my_module.Circle(radius)
print(circle.area())